Simple web application for Apache Point Observatory Night Logs.

Uses a postgresql server. Don't forget to create a .flaskenv file with FLASK_APP=nightlog.py, then use flask run in terminal to start. 
